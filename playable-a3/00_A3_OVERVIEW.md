# MOMEY PLAYABLE A3

狀態：`SOL_LOCAL_PASS / PRE-COMMIT`。Luna 完成主要 implementation；Sol High 已完成獨立 source、rendered、synthetic 與 adversarial review。Deployment／ZIP 狀態仍以實際外部動作後的 final pack 為準。

## 目標

保留 A2 的海岬防洪站共同序章、林芮／高承、18:40／18:47／18:48 時間線、固定真相、公開交換、一次有限查證、二選一決定與四個後果分支；把三支手機的一鍵 reveal 改成短而可修正的角色工具，並在公開交換後只留下 compact 三行共同摘要。

## 執行邊界

- route 只有 `/playable-a3/`，使用靜態 HTML/CSS/JS 與 `localStorage`。
- 寫入範圍只有本目錄；A2 六張 WebP 由 A2 byte-for-byte 複製。
- 三支手機不共享完成狀態；每席只在自己的 storage 內確認工具完成與口頭交換完成。
- 未建立 commit、未 deploy、未建立 ZIP 或 Desktop delivery metadata。

## 玩家可見的 A3 差異

1. 事件指揮：先點流程，再點 t=20／t=95 正確終點；兩段完成後看見封閉切斷救援的時間關係。
2. 救援聯絡：逐項打開裝置簽名、兩點回應、時間邊界、位置／路線四欄；沒有正誤問答。
3. 結構安全：拖動 range 或點時間 marker，把壓力前緣推到 95 秒；不到終點只是中間投影。
4. 公開交換：每席有一個自然問題；team-ready 後只顯示三行 board，不重印三張完整角色 evidence。

## 驗收狀態

Source、syntax、contract、rendered smoke 的結果都以實際命令輸出為準；worker assessment 與 Sol final verdict 分開記錄。
