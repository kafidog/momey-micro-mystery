# Known Limitations

- 22 段 `zm_010` MP3 已生成並技術驗證，owner 已接受暫定使用；這不是永久聲音品牌決策。
- 合成音對台灣繁中人名、數字與技術詞的自然度仍可能低於真人錄音；caption 是完整事實來源。
- static localStorage 狀態不是跨裝置同步；三支 role URL 只共享固定 profile seed，不共享每席 UI state。
- browser autoplay policy 可能阻止播放；caption 與 progression 不受影響。
- 沒有真人三人 first-run 證據；目前是 synthetic first-run。
