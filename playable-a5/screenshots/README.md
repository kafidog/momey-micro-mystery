# Screenshots

Runtime screenshots are generated by `tests/render-smoke.cjs` into an explicitly supplied external evidence directory. Final authoritative images live in the Desktop A5 review pack, not in repository source.

