# Source Snapshot

The final Desktop review pack contains a frozen copy of the A5 runtime, story assets, and tests plus a manifest with SHA-256 values. This repository directory is a marker only; it prevents an old copied snapshot from being mistaken for current source before final packaging.

